import { ElementType } from "react";

export type NavItem = {
  label: string;
  href: string;
  icon: ElementType;
};

export type SocialItem = {
  label: string;
  href: string;
  icon: ElementType;
};
